let famous_person: string = "Imam Shafi'i";

let quote: string = "My heart is at ease knowing that what was meant for me will never miss me, and that what misses me was never meant for me.";

// Compose the message
let message: string = `${famous_person} once said, "${quote}"`;

// Print the message
console.log(message);
