"use strict";
let friendsName = ['Sara', 'Faiza', 'Amna', 'GulJana', 'Maria'];
for (let i = 0; i < friendsName.length; i++) {
    console.log(`${friendsName[i]} Would you like to learn some typescript today?`);
}
